# Frase inicial
frase = "Python es divertido"

# Reemplazar la palabra "divertido" por "genial" usando el método replace()
frase_modificada = frase.replace("divertido", "genial")

# Imprimir el resultado
print(frase_modificada)
